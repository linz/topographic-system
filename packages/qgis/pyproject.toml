[project]
name = "topographic-system-qgis"
version = "0.1.0"
description = "Qgis project for exporting maps in pdf or tif"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "arrow",
    "qgis",
    "ruff>=0.14.6",
]
