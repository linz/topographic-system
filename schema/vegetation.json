{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vegetation",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "UUID for the feature."
    },
    "updated_at": {
      "type": "object",
      "properties": {}
    },
    "created_at": {
      "type": "object",
      "properties": {},
      "description": "Default at write time: today."
    },
    "t50_fid": {},
    "type": {
      "anyOf": [
        {
          "type": "string",
          "const": "exotic"
        },
        {
          "type": "string",
          "const": "native"
        },
        {
          "type": "string",
          "const": "scattered_scrub"
        },
        {
          "type": "string",
          "const": "scrub"
        },
        {
          "type": "null"
        }
      ]
    },
    "subtype": {
      "anyOf": [
        {
          "$ref": "#/$defs/SubTypeEnum"
        },
        {
          "type": "null"
        }
      ]
    },
    "species": {
      "anyOf": [
        {
          "$ref": "#/$defs/SpeciesEnum"
        },
        {
          "type": "null"
        }
      ]
    },
    "geometry": {
      "description": "WKB geometry. Validation only checks presence + non-null; semantic checks live elsewhere.",
      "not": {
        "type": "null"
      }
    },
    "bbox": {
      "$ref": "#/$defs/BBox"
    }
  },
  "required": ["id", "updated_at", "created_at", "geometry"],
  "additionalProperties": false,
  "$defs": {
    "SubTypeEnum": {
      "type": "string",
      "enum": ["coniferous", "non-coniferous"],
      "description": "Auto-derived from observed values of `sub_type`."
    },
    "SpeciesEnum": {
      "type": "string",
      "enum": ["mussels", "salmon"],
      "description": "Auto-derived from observed values of `species`."
    },
    "BBox": {
      "type": "object",
      "properties": {
        "xmin": {
          "type": "number"
        },
        "ymin": {
          "type": "number"
        },
        "xmax": {
          "type": "number"
        },
        "ymax": {
          "type": "number"
        }
      },
      "required": ["xmin", "ymin", "xmax", "ymax"],
      "description": "GeoParquet 1.1 covering bbox struct."
    }
  }
}
