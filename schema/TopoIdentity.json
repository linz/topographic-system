{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "TopoIdentity.json",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "UUID for the feature."
    }
  },
  "required": ["id"],
  "description": "Identity columns shared by every file in the dataset."
}
